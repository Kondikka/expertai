scripted_gui = {
	building_blocker = { #hides the dummy building

		context_type = player_context

		window_name = "building_blocker_window"
		
		parent_window_token = construction_tab

		visible = { always = no }
	}
}