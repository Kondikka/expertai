ideas = {

	mobilization_laws = { 
		law = yes

		disarmed_nation = {

			on_add = { #EAI
				if = { limit = { has_country_flag = EAI_logging } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSCRIPTION LAW: disarmed_nation (normal)" } 
			}
						
			cost = 150
			removal_cost = -1
			level = 7
			
			available = {
				#has_manpower_for_recruit_change_to = { value = 0.005 group = mobilization_laws }
			}

			modifier = {
				conscription = 0.01
			}
			
			cancel_if_invalid = no
			
			ai_will_do = {
				factor = 0
			}
		}
		
		volunteer_only = {

			on_add = { #EAI
				if = { limit = { is_ai = yes }

					# calculate the time it takes to mobilize this mp
					set_temp_variable = { rec_pop_change = 0.5 }
					EAI_calculate_mobilization_duration = yes

					if = { limit = { has_country_flag = EAI_logging } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSCRIPTION LAW: volunteer_only (normal)" } 
				}
			}	

			cost = 150
			removal_cost = -1
			level = 6
			
			available = {
				#has_manpower_for_recruit_change_to =  { value = 0.015 group = mobilization_laws }
				if = {
					limit = {
						original_tag = HUN
						has_dlc = "Death or Dishonor"
					}
					NOT = { has_idea = HUN_treaty_of_triannon }
				}
				NOT = { has_idea = undisturbed_isolation }
				NOT = { has_idea = isolation }
			}

			modifier = {
				conscription = 0.015
			}
			
			default = yes
			
			cancel_if_invalid = no
			
			ai_will_do = {
   				factor = 100000
				
				modifier = { factor = 0 has_country_flag = EAI_mobilizing_manpower }
				modifier = {
					factor = 0
					
					NOT = {
						AND = {
							EAI_can_take_volunteer_only = yes
							
							OR = {
            				    AND = {
							    	has_war = yes
							    }
							    AND = {
							    	has_war = no
				
							    	NOT = {
							    		EAI_can_upgrade_economy_law = yes
				
							    		EAI_can_upgrade_trade_law = yes
							    	}
							    }
            				}
						}
					}
				}
   			}
		}
		
		limited_conscription = {
					
			on_add = { #EAI
				if = { limit = { is_ai = yes }

					# calculate the time it takes to mobilize this mp
					set_temp_variable = { rec_pop_change = 1.0 }
					EAI_calculate_mobilization_duration = yes

					if = { limit = { has_country_flag = EAI_logging } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSCRIPTION LAW: limited_conscription (normal)" } 
				}
			}

			cost = 150
			removal_cost = -1
			level = 5

			available = {
				#has_manpower_for_recruit_change_to =  { value = 0.025 group = mobilization_laws }
				has_war_support > 0.1
				NOT = { has_idea = undisturbed_isolation }
				NOT = { has_idea = isolation }
				if = {
					limit = {
						original_tag = AST
						has_dlc = "Together for Victory"
					}
					has_completed_focus = AST_citizen_military_forces
				}
				if = {
					limit = {
						original_tag = CAN
						has_dlc = "Together for Victory"
					}
					has_completed_focus = CAN_commit_to_the_war
				}
				if = {
					limit = {
						original_tag = RAJ
						has_dlc = "Together for Victory"
					}
					is_subject = no
				}

				if = {
					limit = {
						original_tag = HUN
						has_dlc = "Death or Dishonor"
					}
					NOT = { has_idea = HUN_treaty_of_triannon }
				}
				if = {
					limit = {
						original_tag = ENG
						has_dlc = "Man the Guns"
						has_completed_focus = ENG_concessions_to_the_trade_unions
					}
					NOT = { has_country_flag = ENG_cannot_increase_conscription_law }
				}
			}

			modifier = {
				conscription = 0.025
			}

			ai_will_do = {
   				factor = 100000
				
				modifier = { factor = 0 has_country_flag = EAI_mobilizing_manpower }
				modifier = {
					factor = 0
					
					NOT = {
						AND = {
							EAI_can_take_limited_conscription = yes
							
							OR = {
            				    AND = {
							    	has_war = yes
							    }
							    AND = {
							    	has_war = no
				
							    	NOT = {
							    		EAI_can_upgrade_economy_law = yes
				
							    		EAI_can_upgrade_trade_law = yes
							    	}
							    }
            				}
						}
					}
				}
   			}
			
			cancel_if_invalid = no
		}
		
		extensive_conscription = {
			
			on_add = { #EAI
				if = { limit = { is_ai = yes }

					# calculate the time it takes to mobilize this mp
					set_temp_variable = { rec_pop_change = 2.5 }
					EAI_calculate_mobilization_duration = yes

					if = { limit = { has_country_flag = EAI_logging } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSCRIPTION LAW: extensive_conscription (normal)" } 
				}
			}

			available = {
				#has_manpower_for_recruit_change_to = { value = 0.05 group = mobilization_laws }
				OR = {
					has_government = fascism
					has_government = communism
					AND = {
						has_war = yes
						enemies_strength_ratio > 0.5
						#any_enemy_country = {
						#	strength_ratio = {
						#		tag = ROOT 
						#		ratio > 0.5
						#	}
						#}
					}					
				}
				has_war_support > 0.2
				#Special rules AST in TFV
				if = {
					limit = {
						original_tag = AST
						has_dlc = "Together for Victory"
					}
					has_completed_focus = AST_citizen_military_forces
					has_completed_focus = AST_fight_work_or_perish
				}				
				if = {
					limit = {
						original_tag = CAN
						has_dlc = "Together for Victory"
					}
					has_completed_focus = CAN_commit_to_the_war
					has_completed_focus = CAN_if_day
				}
				if = {
					limit = {
						original_tag = RAJ
						has_dlc = "Together for Victory"
					}
					is_subject = no
				}

				if = {
					limit = {
						original_tag = HUN
						has_dlc = "Death or Dishonor"
					}
					NOT = { has_idea = HUN_treaty_of_triannon }
				}
				if = {
					limit = {
						original_tag = ENG
						has_dlc = "Man the Guns"
						has_completed_focus = ENG_concessions_to_the_trade_unions
					}
					NOT = { has_country_flag = ENG_cannot_increase_conscription_law }
				}
			}
			
			cost = 150
			removal_cost = -1
			level = 4
			
			modifier = {
				conscription = 0.05
				#industrial_capacity_factory = -0.1
				#production_speed_buildings_factor = -0.1
				training_time_factor = 0.1
			}
			
			cancel_if_invalid = no
			
			ai_will_do = {
   				factor = 100000
				
				modifier = { factor = 0 has_country_flag = EAI_mobilizing_manpower }
				modifier = {
					factor = 0
					
					NOT = {
						AND = {
							EAI_can_take_extensive_conscription = yes
							
							OR = {
            				    AND = {
							    	has_war = yes
							    }
							    AND = {
							    	has_war = no
				
							    	NOT = {
							    		EAI_can_upgrade_economy_law = yes
				
							    		EAI_can_upgrade_trade_law = yes
							    	}
							    }
            				}
						}
					}
				}
   			}
		}
		
		service_by_requirement = {
			
			on_add = { #EAI
				if = { limit = { is_ai = yes }

					# calculate the time it takes to mobilize this mp
					set_temp_variable = { rec_pop_change = 5.0 }
					EAI_calculate_mobilization_duration = yes

					if = { limit = { has_country_flag = EAI_logging } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSCRIPTION LAW: service_by_requirement (normal)" } 
				}
			}
			
			available = {
				#has_manpower_for_recruit_change_to =  { value = 0.1 group = mobilization_laws }
				OR = {
					has_government = fascism
					has_government = communism
					AND = {
						has_war = yes
						enemies_strength_ratio > 0.6
						#any_enemy_country = {
						#	strength_ratio = {
						#		tag = ROOT 
						#		ratio > 0.6
						#	}
						#}
					}
				}
				OR = {
					has_war_support > 0.6
					surrender_progress > 0
				}

				if = {
					limit = {
						original_tag = AST
						has_dlc = "Together for Victory"
					}
					has_completed_focus = AST_citizen_military_forces
					has_completed_focus = AST_fight_work_or_perish
				}

				if = {
					limit = {
						original_tag = CAN
						has_dlc = "Together for Victory"
					}
					OR = {
						has_completed_focus = CAN_compromise_with_quebec
						has_completed_focus = CAN_forced_quebec_conscription
					}
				}
				if = {
					limit = {
						original_tag = RAJ
						has_dlc = "Together for Victory"
					}
					is_subject = no
				}

				if = {
					limit = {
						original_tag = HUN
						has_dlc = "Death or Dishonor"
					}
					NOT = { has_idea = HUN_treaty_of_triannon }
				}
				if = {
					limit = {
						original_tag = ENG
						has_dlc = "Man the Guns"
						has_completed_focus = ENG_concessions_to_the_trade_unions
					}
					NOT = { has_country_flag = ENG_cannot_increase_conscription_law }
				}
			}
			
			cost = 150
			removal_cost = -1
			level = 3

			modifier = {
				conscription = 0.1
				industrial_capacity_factory = -0.1
				industrial_capacity_dockyard = -0.1
				production_speed_buildings_factor = -0.1
				training_time_factor = 0.2
			}
			
			cancel_if_invalid = no
			
			ai_will_do = {
   				factor = 100000
				
				modifier = { factor = 0 has_country_flag = EAI_mobilizing_manpower }
				modifier = {
					factor = 0
					
					NOT = {
						AND = {
							EAI_can_take_service_by_requirement = yes
							
							OR = {
            				    AND = {
							    	has_war = yes
							    }
							    AND = {
							    	has_war = no
				
							    	NOT = {
							    		EAI_can_upgrade_economy_law = yes
				
							    		EAI_can_upgrade_trade_law = yes
							    	}
							    }
            				}
						}
					}
				}
   			}
		}
		
		all_adults_serve = {
			
			on_add = { #EAI
				if = { limit = { is_ai = yes }

					# calculate the time it takes to mobilize this mp
					set_temp_variable = { rec_pop_change = 10.0 }
					EAI_calculate_mobilization_duration = yes

					if = { limit = { has_country_flag = EAI_logging } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSCRIPTION LAW: all_adults_serve (normal)" } 
				}
			}
			
			available = {
				#has_manpower_for_recruit_change_to =  { value = 0.2 group = mobilization_laws }
				has_war = yes
				OR = {
					has_war_support > 0.7
					surrender_progress > 0
				}
				enemies_strength_ratio > 0.75
				#any_enemy_country = {
				#	strength_ratio = {
				#		tag = ROOT 
				#		ratio > 0.75
				#	}
				#}

				if = {
					limit = {
						original_tag = AST
						has_dlc = "Together for Victory"
					}
					has_completed_focus = AST_citizen_military_forces
					has_completed_focus = AST_fight_work_or_perish
				}

				if = {
					limit = {
						original_tag = CAN
						has_dlc = "Together for Victory"
					}
					OR = {
						has_completed_focus = CAN_compromise_with_quebec
						has_completed_focus = CAN_forced_quebec_conscription
					}					
				}
				if = {
					limit = {
						original_tag = RAJ
						has_dlc = "Together for Victory"
					}
					is_subject = no
				}

				if = {
					limit = {
						original_tag = HUN
						has_dlc = "Death or Dishonor"
					}
					NOT = { has_idea = HUN_treaty_of_triannon }
				}
				if = {
					limit = {
						original_tag = ENG
						has_dlc = "Man the Guns"
						has_completed_focus = ENG_concessions_to_the_trade_unions
					}
					NOT = { has_country_flag = ENG_cannot_increase_conscription_law }
				}
			}
		
			cost = 150
			removal_cost = -1
			level = 2
			
			modifier = {
				conscription = 0.2
				industrial_capacity_factory = -0.3
				industrial_capacity_dockyard = -0.3
				production_speed_buildings_factor = -0.3
				#XP from reinforcement penalty (small)
				training_time_factor = 0.3
			}
			
			cancel_if_invalid = no
			
			ai_will_do = {
   				factor = 100000
				
				modifier = { factor = 0 has_country_flag = EAI_mobilizing_manpower }
				modifier = {
					factor = 0
					
					NOT = {
						AND = {
							EAI_can_take_all_adults_serve = yes
							
							OR = {
            				    AND = {
							    	has_war = yes
							    }
							    AND = {
							    	has_war = no
				
							    	NOT = {
							    		EAI_can_upgrade_economy_law = yes
				
							    		EAI_can_upgrade_trade_law = yes
							    	}
							    }
            				}
						}
					}
				}
   			}
		}
		
		scraping_the_barrel = {
			
			on_add = { #EAI
				if = { limit = { is_ai = yes }

					# calculate the time it takes to mobilize this mp
					set_temp_variable = { rec_pop_change = 5.0 }
					EAI_calculate_mobilization_duration = yes

					if = { limit = { has_country_flag = EAI_logging } log = "[GetYear] [GetMonth] | AI | [Root.GetName] | CONSCRIPTION LAW: scraping_the_barrel (normal)" } 
				}
			}
			
			available = {
				#has_manpower_for_recruit_change_to =  { value = 0.25 group = mobilization_laws }
				has_war = yes
				OR = {
					has_war_support > 0.85
					surrender_progress > 0.25
				}
				enemies_strength_ratio > 1
				#any_enemy_country = {
				#	strength_ratio = {
				#		tag = ROOT 
				#		ratio > 1
				#	}
				#}
				if = {
					limit = {
						original_tag = AST
						has_dlc = "Together for Victory"
					}
					has_completed_focus = AST_citizen_military_forces
					has_completed_focus = AST_fight_work_or_perish
				}

				if = {
					limit = {
						original_tag = CAN
						has_dlc = "Together for Victory"
					}
					OR = {
						has_completed_focus = CAN_compromise_with_quebec
						has_completed_focus = CAN_forced_quebec_conscription
					}
				}
				if = {
					limit = {
						original_tag = RAJ
						has_dlc = "Together for Victory"
					}
					is_subject = no
				}

				if = {
					limit = {
						original_tag = HUN
						has_dlc = "Death or Dishonor"
					}
					NOT = { has_idea = HUN_treaty_of_triannon }
				}
				if = {
					limit = {
						original_tag = ENG
						has_dlc = "Man the Guns"
						has_completed_focus = ENG_concessions_to_the_trade_unions
					}
					NOT = { has_country_flag = ENG_cannot_increase_conscription_law }
				}
			}
			
			cost = 150
			removal_cost = -1
			level = 1

			modifier = {
				conscription = 0.25
				industrial_capacity_factory = -0.4
				industrial_capacity_dockyard = -0.4
				production_speed_buildings_factor = -0.4
				training_time_factor = 0.5
			}
			
			cancel_if_invalid = no
			
			ai_will_do = {
   				factor = 100000
				
				modifier = { factor = 0 has_country_flag = EAI_mobilizing_manpower }
				modifier = {
					factor = 0
					
					NOT = {
						AND = {
							EAI_can_take_scraping_the_barrel = yes
							
							OR = {
            				    AND = {
							    	has_war = yes
							    }
							    AND = {
							    	has_war = no

							    	NOT = {
							    		EAI_can_upgrade_economy_law = yes

							    		EAI_can_upgrade_trade_law = yes
							    	}
							    }
            				}
						}
					}
				}
   			}
		}
	}
}
